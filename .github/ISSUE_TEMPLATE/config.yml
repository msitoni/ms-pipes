blank_issues_enabled: false
issue_templates:
  - name: "AWS ECS"
    description: "Criar um repositório novo com a estrutura básica do Terraform para um AWS ECS"
    labels: ["terraform", "ecs", "infra"]
    file: aws-ecs.yml

  - name: "AWS SQS"
    description: "Criar um repositório novo com a estrutura básica do Terraform para um AWS SQS"
    labels: ["terraform", "sqs", "infra"]
    file: aws-sqs.yml
